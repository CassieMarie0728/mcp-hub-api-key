"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from .httpclient import AsyncHttpClient, ClientOwner, HttpClient, close_clients
from .sdkconfiguration import SDKConfiguration
from .utils.logger import Logger, get_default_logger
from .utils.retries import RetryConfig
from cassie_marie_mcp_hub_api_python import models, utils
from cassie_marie_mcp_hub_api_python._hooks import SDKHooks
from cassie_marie_mcp_hub_api_python.types import OptionalNullable, UNSET
import httpx
import importlib
from typing import Any, Callable, Dict, Optional, TYPE_CHECKING, Union, cast
import weakref

if TYPE_CHECKING:
    from cassie_marie_mcp_hub_api_python.auth import Auth
    from cassie_marie_mcp_hub_api_python.capabilities import Capabilities
    from cassie_marie_mcp_hub_api_python.completions import Completions
    from cassie_marie_mcp_hub_api_python.connectionauth import ConnectionAuth
    from cassie_marie_mcp_hub_api_python.connections import Connections
    from cassie_marie_mcp_hub_api_python.contexts import Contexts
    from cassie_marie_mcp_hub_api_python.events import Events
    from cassie_marie_mcp_hub_api_python.logging import Logging
    from cassie_marie_mcp_hub_api_python.prompts import Prompts
    from cassie_marie_mcp_hub_api_python.queries import Queries
    from cassie_marie_mcp_hub_api_python.resources import Resources
    from cassie_marie_mcp_hub_api_python.runtime import Runtime
    from cassie_marie_mcp_hub_api_python.servers import Servers
    from cassie_marie_mcp_hub_api_python.system import System
    from cassie_marie_mcp_hub_api_python.tools import Tools


class McpHubAPIPython(BaseSDK):
    r"""MCP Hub API: MCP Hub API provides a unified wrapper for discovering MCP servers, creating and managing connections, inspecting capabilities, invoking tools, reading resources, retrieving prompts, streaming events, and handling user/session authentication.

    This contract is designed for both browser-based app sessions and programmatic clients, with support for cookie-based sessions and bearer-token authentication.
    """

    system: "System"
    r"""System related endpoints"""
    auth: "Auth"
    r"""Authentication and user session management"""
    servers: "Servers"
    r"""MCP server discovery and management"""
    connections: "Connections"
    r"""MCP connection lifecycle management"""
    capabilities: "Capabilities"
    r"""Capability and server metadata inspection"""
    tools: "Tools"
    r"""Access to related tools integrated within MCP Hub"""
    resources: "Resources"
    r"""Resource management and subscription"""
    prompts: "Prompts"
    r"""Prompt listing and retrieval"""
    completions: "Completions"
    r"""Completion requests"""
    logging: "Logging"
    r"""Logging level management"""
    events: "Events"
    r"""Event streaming and request inspection"""
    queries: "Queries"
    r"""Sending queries and commands to MCP servers"""
    contexts: "Contexts"
    r"""MCP server contexts and models management"""
    runtime: "Runtime"
    r"""Per-connection MCP runtime operations such as initialize, tool invocation, and cancellation"""
    connection_auth: "ConnectionAuth"
    r"""Per-connection authentication bootstrap and status"""
    _sub_sdk_map = {
        "system": ("cassie_marie_mcp_hub_api_python.system", "System"),
        "auth": ("cassie_marie_mcp_hub_api_python.auth", "Auth"),
        "servers": ("cassie_marie_mcp_hub_api_python.servers", "Servers"),
        "connections": ("cassie_marie_mcp_hub_api_python.connections", "Connections"),
        "capabilities": (
            "cassie_marie_mcp_hub_api_python.capabilities",
            "Capabilities",
        ),
        "tools": ("cassie_marie_mcp_hub_api_python.tools", "Tools"),
        "resources": ("cassie_marie_mcp_hub_api_python.resources", "Resources"),
        "prompts": ("cassie_marie_mcp_hub_api_python.prompts", "Prompts"),
        "completions": ("cassie_marie_mcp_hub_api_python.completions", "Completions"),
        "logging": ("cassie_marie_mcp_hub_api_python.logging", "Logging"),
        "events": ("cassie_marie_mcp_hub_api_python.events", "Events"),
        "queries": ("cassie_marie_mcp_hub_api_python.queries", "Queries"),
        "contexts": ("cassie_marie_mcp_hub_api_python.contexts", "Contexts"),
        "runtime": ("cassie_marie_mcp_hub_api_python.runtime", "Runtime"),
        "connection_auth": (
            "cassie_marie_mcp_hub_api_python.connectionauth",
            "ConnectionAuth",
        ),
    }

    def __init__(
        self,
        app_session_cookie: Optional[
            Union[Optional[str], Callable[[], Optional[str]]]
        ] = None,
        server_idx: Optional[int] = None,
        server_url: Optional[str] = None,
        url_params: Optional[Dict[str, str]] = None,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Logger] = None,
    ) -> None:
        r"""Instantiates the SDK configuring it with the provided parameters.

        :param app_session_cookie: The app_session_cookie required for authentication
        :param server_idx: The index of the server to use for all methods
        :param server_url: The server URL to use for all methods
        :param url_params: Parameters to optionally template the server URL with
        :param client: The HTTP client to use for all synchronous methods
        :param async_client: The Async HTTP client to use for all asynchronous methods
        :param retry_config: The retry configuration to use for all supported methods
        :param timeout_ms: Optional request timeout applied to each operation in milliseconds
        """
        client_supplied = True
        if client is None:
            client = httpx.Client()
            client_supplied = False

        assert issubclass(
            type(client), HttpClient
        ), "The provided client must implement the HttpClient protocol."

        async_client_supplied = True
        if async_client is None:
            async_client = httpx.AsyncClient()
            async_client_supplied = False

        if debug_logger is None:
            debug_logger = get_default_logger()

        assert issubclass(
            type(async_client), AsyncHttpClient
        ), "The provided async_client must implement the AsyncHttpClient protocol."

        security: Any = None
        if callable(app_session_cookie):
            # pylint: disable=unnecessary-lambda-assignment
            security = lambda: models.Security(app_session_cookie=app_session_cookie())
        else:
            security = models.Security(app_session_cookie=app_session_cookie)

        if server_url is not None:
            if url_params is not None:
                server_url = utils.template_url(server_url, url_params)

        BaseSDK.__init__(
            self,
            SDKConfiguration(
                client=client,
                client_supplied=client_supplied,
                async_client=async_client,
                async_client_supplied=async_client_supplied,
                security=security,
                server_url=server_url,
                server_idx=server_idx,
                retry_config=retry_config,
                timeout_ms=timeout_ms,
                debug_logger=debug_logger,
            ),
        )

        hooks = SDKHooks()

        # pylint: disable=protected-access
        self.sdk_configuration.__dict__["_hooks"] = hooks

        self.sdk_configuration = hooks.sdk_init(self.sdk_configuration)

        weakref.finalize(
            self,
            close_clients,
            cast(ClientOwner, self.sdk_configuration),
            self.sdk_configuration.client,
            self.sdk_configuration.client_supplied,
            self.sdk_configuration.async_client,
            self.sdk_configuration.async_client_supplied,
        )

    def __getattr__(self, name: str):
        if name in self._sub_sdk_map:
            module_path, class_name = self._sub_sdk_map[name]
            try:
                module = importlib.import_module(module_path)
                klass = getattr(module, class_name)
                instance = klass(self.sdk_configuration)
                setattr(self, name, instance)
                return instance
            except ImportError as e:
                raise AttributeError(
                    f"Failed to import module {module_path} for attribute {name}: {e}"
                ) from e
            except AttributeError as e:
                raise AttributeError(
                    f"Failed to find class {class_name} in module {module_path} for attribute {name}: {e}"
                ) from e

        raise AttributeError(
            f"'{type(self).__name__}' object has no attribute '{name}'"
        )

    def __dir__(self):
        default_attrs = list(super().__dir__())
        lazy_attrs = list(self._sub_sdk_map.keys())
        return sorted(list(set(default_attrs + lazy_attrs)))

    def __enter__(self):
        return self

    async def __aenter__(self):
        return self

    def __exit__(self, exc_type, exc_val, exc_tb):
        if (
            self.sdk_configuration.client is not None
            and not self.sdk_configuration.client_supplied
        ):
            self.sdk_configuration.client.close()
        self.sdk_configuration.client = None

    async def __aexit__(self, exc_type, exc_val, exc_tb):
        if (
            self.sdk_configuration.async_client is not None
            and not self.sdk_configuration.async_client_supplied
        ):
            await self.sdk_configuration.async_client.aclose()
        self.sdk_configuration.async_client = None
